import React from "react";
import ReactDOM from "react-dom/client";
import PostpartumApp from "./PostpartumApp";
import "./index.css";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
  <React.StrictMode>
    <PostpartumApp />
  </React.StrictMode>
);
